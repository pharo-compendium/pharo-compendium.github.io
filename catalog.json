---
layout: null
---
[
    {% for entry in site.posts %}
        {
            "title": "{{ entry.title }}", 
            "date": "{{ entry.date }}", 
            "version": "{{ entry.version }}"       
        }
        {% unless forloop.last %},{% endunless %}
    {% endfor %}
]