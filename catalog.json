---
layout: null
---
[
    {% for entry in site.entries %}
        {
            "title": "{{ entry.title }}", 
            "date": "{{ entry.date }}", 
            "version": "{{ entry.version }}"       
        }
        {% unless forloop.last %},{% endunless %}
    {% endfor %}
]