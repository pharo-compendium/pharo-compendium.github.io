---
layout: null
---
[
    {% for post in site.posts %}
        {
            "title": "{{ post.title }}", 
            "date": "{{ post.date }}", 
            "version": "{{ post.version }}"       
        }
        {% unless forloop.last %},{% endunless %}
    {% endfor %}
]