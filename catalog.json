---
layout: null
---
[
    {% for entry in site.posts %}
        {
            "title": "{{ entry.title }}",              
            "url": "{{ site.BASE_PATH }}{{ entry.url }}",
            "category": "{{ entry.category }}",                                      
            "homepage": "{{ entry.homepage }}",        
            "icon": "{{ entry.icon }}",
            "expression": "{{ entry.expression }}"                
        }
        {% unless forloop.last %},{% endunless %}
    {% endfor %}
]